# Strategy Proposals — Rejected — India

Human moves a proposal here when rejecting. The 3-rejections / 30-day suppression
(enforced by `scripts/proposal-check.sh`) reads this file.

Format (one line per rejection):
```
- YYYY-MM-DD · dim:<dimension_slug> · from: STRATEGY-PROPOSALS.md#<anchor> · reason: "<one-line>"
```

The `dim:` field must match the `dimension:` field in the original proposal.

(empty)
